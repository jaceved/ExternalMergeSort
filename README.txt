1) Execute the datamaker.py to generate the binary file
	* This is a python file	
	* The binary file will be named  "relation.bin" 

2) Execute EMS.java 
	* make sure all files are in the same directory
	* EMS contains the main. Read description of class to make changes to buffer size and other misc. calculations

3) output files should be generated in same directory 